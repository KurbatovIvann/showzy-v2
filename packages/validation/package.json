{
  "name": "@showzy/validation",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Client-safe shared Zod schemas for action contracts and apps (ADR-0016, contract.md §2). Domain slices are exported as subpaths; this package owns no actions or tables.",
  "exports": {
    "./catalog": "./src/catalog.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@showzy/tooling": "workspace:*",
    "@types/node": "^22.20.1",
    "eslint": "^10.8.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.10"
  }
}
