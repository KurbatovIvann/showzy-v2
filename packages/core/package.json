{
  "name": "@showzy/core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Action runtime: contracts, registry, principal contexts, execution pipeline, events, idempotency, confirmation, audit, typed errors, and the module test kit. Only the client-safe ./contract subpath exists so far (fnd-T8).",
  "exports": {
    "./contract": "./src/contract/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@showzy/tooling": "workspace:*",
    "eslint": "^10.8.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.10"
  }
}
