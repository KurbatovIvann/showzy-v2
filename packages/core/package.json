{
  "name": "@showzy/core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Action runtime: contracts, registry, principal contexts, execution pipeline, events, idempotency, confirmation, audit, typed errors, and the module test kit. Exists so far: ./contract (fnd-T8), ./errors + implementAction/registry at the root (fnd-T9).",
  "exports": {
    ".": "./src/index.ts",
    "./contract": "./src/contract/index.ts",
    "./errors": "./src/errors/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@showzy/tooling": "workspace:*",
    "eslint": "^10.8.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.10"
  }
}
