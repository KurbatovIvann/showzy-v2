{
  "name": "@showzy/core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Action runtime: contracts, registry, principal contexts, execution pipeline, events, idempotency, confirmation, audit, typed errors, and the module test kit. Exists so far: ./contract (fnd-T8), ./errors + implementAction/registry at the root (fnd-T9), contract check (fnd-T10), principal context factories (fnd-T11), execution pipeline (fnd-T12), audit protocol (fnd-T13), rate limiting (fnd-T14), idempotency protocol (fnd-T15), domain event emission (fnd-T16), delivery (fnd-T17), retry/dead-letter/replay (fnd-T18), ctx.call (fnd-T19), ctx.callAtomic (fnd-T19A), the confirmation protocol (fnd-T20), and the module test kit at ./testing (fnd-T21/T22).",
  "exports": {
    ".": "./src/index.ts",
    "./contract": "./src/contract/index.ts",
    "./errors": "./src/errors/index.ts",
    "./testing": "./src/testing/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit && tsc -p tsconfig.contract.json --noEmit",
    "lint": "eslint .",
    "test": "vitest run",
    "test:unit": "vitest run --project unit"
  },
  "dependencies": {
    "@showzy/db": "workspace:*",
    "drizzle-orm": "^0.45.2",
    "pino": "^10.3.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@showzy/tooling": "workspace:*",
    "@types/node": "^22.20.1",
    "eslint": "^10.8.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.10"
  }
}
