{
  "name": "@showzy/config",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Validated runtime environment plus process log/Sentry redaction: Zod-parsed process.env, fail-fast at boot, secret values never echoed in errors or logs.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run",
    "test:unit": "vitest run"
  },
  "dependencies": {
    "pino": "^10.3.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@showzy/tooling": "workspace:*",
    "@types/node": "^22.20.1",
    "eslint": "^10.8.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.10"
  }
}
