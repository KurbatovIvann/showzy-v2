// fnd-T2 meta-verification: this file deliberately violates Prettier style so
// the CI `checks` job must fail on this throwaway branch. Never merge.
export const seededViolation =   {   answer:42,    reason:    "format check must turn red"   }
