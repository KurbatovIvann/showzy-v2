{
  "name": "@showzy/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Outbox poller and delivery executor: LISTEN/NOTIFY with polling fallback, claim drain on shutdown, idempotency-key expiry cleanup (fnd-T27).",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run",
    "start": "node --experimental-strip-types --import ../../packages/db/scripts/ts-resolve-register.mjs --env-file=../../.env src/index.ts"
  },
  "dependencies": {
    "@sentry/node": "^10.70.0",
    "@showzy/chat": "workspace:*",
    "@showzy/config": "workspace:*",
    "@showzy/core": "workspace:*",
    "@showzy/db": "workspace:*",
    "ioredis": "^6.0.0",
    "pg": "^8.23.0",
    "pino": "^10.3.1"
  },
  "devDependencies": {
    "@showzy/tooling": "workspace:*",
    "@types/node": "^22.20.1",
    "drizzle-orm": "^0.45.2",
    "@types/pg": "^8.23.0",
    "eslint": "^10.8.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.10",
    "zod": "^4.4.3"
  }
}
