# Archived domain specs

**Stop.** This tree is not authority (ADR-0023, ADR-0033). Agents must not
read these files unless a human named one. Reconstruct v1 from
`docs/reference/`. New work uses `/feature`, `*.contract.ts`, and goldens.

Protocol manuals that still describe frozen packages remain in
`docs/specs/` (`core`, `db`, `contract`, `money`, `security-operations`,
`companies-foundation`).
