# Archived domain specs

Research only. **Not authority** (ADR-0023).

These files were Living/Active SDD novels. Planner may read them the same
way as `docs/reference/` when reconstructing v1 behavior. New work uses
`/feature`, `*.contract.ts`, and the golden slices.

Protocol manuals that still describe frozen packages remain in
`docs/specs/` (`core`, `db`, `contract`, `money`, `security-operations`,
`companies-foundation`).
