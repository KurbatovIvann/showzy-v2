# Historical implementation plans

These files are **archived**. They are not contracts and must not be
executed. Agents must not open `docs/archive/plans/` (ADR-0033).

New work does not add `docs/plans/<module>.md` (ADR-0023). Planner output
is a Linear feature card and tickets. The executable contract is the
feature card plus `*.contract.ts`.
