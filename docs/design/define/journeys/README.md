# Journeys

Greenfield classic/AI journey docs were archived to
`docs/archive/design/define/journeys/`. They are not authority.

Current sources: `docs/design/inventory/` and `docs/design/mapping/`.
New journey docs are written only when a vertical slice is about to get
product screens, after the UX gate.
