# Design system

Greenfield token and component contracts were archived to
`docs/archive/design/system/`. They are not authority.

Current source: `docs/design/inventory/`. Rebuild tokens and components from
the V1 inventory when product UI work starts.
