# Applies to every image built from the repo root (apps/web/Dockerfile).
# Secrets and machine-local state must never enter a build context.
.env
.env.*
!.env.example
.git
node_modules
**/node_modules
**/dist
**/.turbo
.turbo
